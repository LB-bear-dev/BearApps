#pragma once
#include <string>
#include <map>
#include <list>
#include <unordered_map>
#include <vector>
#include <filesystem>
#include "Coord.h"

using STR = std::string;
using STRID = std::string;
using STRIDR = const STRID&;
using PATH = std::filesystem::path;
using PATHR = const PATH&;